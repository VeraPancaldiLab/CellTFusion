% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CellTFusion.R
\name{cell.groups.stat.analysis}
\alias{cell.groups.stat.analysis}
\title{Perform statistical analysis on cell group scores using a specified test}
\usage{
cell.groups.stat.analysis(
  cell.groups,
  coldata,
  trait,
  method = c("fisher", "wilcox", "anova", "kruskal", "ttest"),
  pval = 0.05
)
}
\arguments{
\item{cell.groups}{A list where:
\itemize{
\item The first element is a data frame or matrix containing cell group scores
(rows = samples, columns = cell groups).
\item The second and third elements contain corresponding metadata or annotations
for these groups (e.g., names, features, etc.).
}}

\item{coldata}{A data frame containing clinical or experimental metadata for samples.
Must include the column specified in \code{trait}.}

\item{trait}{Character. The name of the column in \code{coldata} representing the clinical
or experimental trait to test against (e.g., response, subtype, etc.).}

\item{method}{Character. Statistical test to perform. One of:
\itemize{
\item \code{"fisher"} — Fisher’s exact test (for categorical data)
\item \code{"wilcox"} — Wilcoxon rank-sum test (non-parametric, binary traits)
\item \code{"anova"} — One-way ANOVA (parametric, >2 groups)
\item \code{"kruskal"} — Kruskal–Wallis test (non-parametric, >2 groups)
\item \code{"ttest"} — Student’s t-test (parametric, binary traits)
}
Defaults to all available options, but only one can be used per call.}

\item{pval}{Numeric. P-value threshold for significance (default: 0.05).}
}
\value{
A list of significant cell groups, where:
\itemize{
\item The first element contains the subset of the original score matrix
for significant features.
\item The second and third elements contain associated metadata or feature
annotations.
}
Returns \code{NULL} if no significant features are found.
}
\description{
This function provides a unified interface to perform one of several
statistical tests (Fisher’s exact, Wilcoxon rank-sum, ANOVA, Kruskal–Wallis,
or Student’s t-test) on cell group scores in relation to a given clinical or
experimental trait. The choice of test is specified by the \code{method} argument.
Each test identifies significant associations and saves a corresponding
visualization for each significant feature in the "Results/" directory.
}
\details{
The function automatically calls the corresponding statistical test function
based on the \code{method} argument:
\itemize{
\item \code{cell.groups.fisher.test()}
\item \code{cell.groups.wilcox.test()}
\item \code{cell.groups.anova.test()}
\item \code{cell.groups.kruskal.test()}
\item \code{cell.groups.ttest()}
}

Each test produces both a statistical result and visual outputs (PDF plots)
stored in the \code{"Results/"} folder. These visualizations include the relevant
test results (p-values) annotated on the plots.
}
\examples{
\dontrun{
# Example usage:
sig.groups <- cell.groups.stat.analysis(cell.groups, coldata, trait = "response",
                                        method = "kruskal", pval = 0.05)
}

}
