% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CellTFusion.R
\name{compute.metada.association}
\alias{compute.metada.association}
\title{Compute associations between TF module scores and clinical metadata}
\usage{
compute.metada.association(
  tfs.modules,
  coldata,
  pval = 0.05,
  width = 20,
  height = 8
)
}
\arguments{
\item{tfs.modules}{A numeric matrix or data frame of TF module scores across samples.
Typically the output from \code{compute.WTCNA()}. Rows represent samples, columns represent TF modules.}

\item{coldata}{A data frame containing clinical traits (both categorical and numerical) for the same samples.
Row names should match those of \code{tfs.modules}.}

\item{pval}{A numeric threshold (default = 0.05) to determine statistical significance.
Only associations with p-values below this threshold are considered significant in the heatmap.}

\item{width}{A numeric value indicating the width (in inches) of the output heatmap plot (default = 20).}

\item{height}{A numeric value indicating the height (in inches) of the output heatmap plot (default = 8).}
}
\value{
This function saves the following to the \verb{Results/} directory:
\itemize{
\item A labeled heatmap showing Pearson correlations and ANOVA test p-values.
\item Individual violin plots for significant categorical trait associations.
}
The function does not return an object to the R environment.
}
\description{
This function tests for associations between transcription factor (TF) module scores
and available clinical traits. It uses Pearson correlation for continuous (numeric) traits,
and ANOVA for categorical traits. Results are visualized as a labeled heatmap and violin plots.
All plots are saved in the \verb{Results/} directory.
}
\examples{

data("network")
data("traitdata")

compute.metada.association(
  tfs.modules = network[[1]],
  coldata = traitdata,
  pval = 0.05,
  width = 15,
  height = 10
)

}
