% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute.bootstrap.ML.R
\name{compute.bootstrap.ML}
\alias{compute.bootstrap.ML}
\title{Compute machine learning models across different splits of train and test}
\usage{
compute.bootstrap.ML(
  raw.counts,
  normalized = F,
  clinical,
  trait,
  trait.positive,
  partition = 0.8,
  metric = "Accuracy",
  iterations,
  feature.selection = F,
  stack,
  deconv_methods = c("Quantiseq", "MCP", "xCell", "CBSX", "Epidish", "DeconRNASeq",
    "DWLS"),
  workers = NULL,
  file.name = NULL,
  return = F
)
}
\arguments{
\item{raw.counts}{A matrix with raw counts (genes symbols as rows and samples as columns)}

\item{normalized}{A boolean value to specify if raw counts have to be normalized (this should always be true unless the provided raw.counts are already the normalized ones)}

\item{clinical}{A data frame with the clinical data containing the target variable to analyze}

\item{trait}{Column name of target variable}

\item{trait.positive}{Value of target consider as positive for prediction}

\item{partition}{Proportion of data going to train}

\item{metric}{Metric to based the methods choice during cross validation (CV). Either accuracy or AUC}

\item{iterations}{Number of iterations to perform}

\item{feature.selection}{If feature selection using Boruta algorithm needs to be computed}

\item{stack}{If stacking needs to be applied}

\item{deconv_methods}{Deconvolution methods to run during CellTFusion}

\item{workers}{Number of workers to use for parallelization}

\item{file.name}{File name for plots}

\item{return}{Return the plots printed during each iteration}
}
\value{
Machine learning models are saved in Results/ folder as .rds files
}
\description{
Compute machine learning models across different splits of train and test
}
\examples{

compute.bootstrap.ML(raw.counts, normalized = T, clinical, trait = "Response", trait.positive = "YES", partition = 0.8, metric = "Accuracy", iterations = 20, feature.selection = F, stack = T, workers = 4, file.name = "Test", return = F)

}
