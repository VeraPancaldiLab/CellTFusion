% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CellTFusion.R
\name{identify.cell.signatures}
\alias{identify.cell.signatures}
\title{Identify cell signatures from Machine Learning Models and Deconvolution Data}
\usage{
\method{identify}{cell.signatures}(model, deconvolution, var_importance, n_top = 20, sign, file.name)
}
\arguments{
\item{model}{A list containing the machine learning model results, including
subgroup compositions and cell groups.}

\item{deconvolution}{A data.frame or matrix of deconvolution features where columns
represent cell types or subgroups.}

\item{var_importance}{A data.frame or tibble containing variable importance scores
(e.g., SHAP values) for the model features.}

\item{n_top}{Integer. Number of top features to select based on importance and direction.
Defaults to 20.}

\item{sign}{Character. Direction of effect to select features: "Increase" or "Decrease".}

\item{file.name}{Character. Base file name for saving PDF plots of results.}
}
\value{
No return value. The function saves several PDF plots into the "Results" directory
and prints progress information during execution.
}
\description{
This function extracts cell subgroup compositions from a machine learning model,
identifies top predictive features based on variable importance, and analyzes
cell-type presence patterns using clustering. It computes feature importance based on
clustering impact and generates visualizations for cell feature presence and TF module scores.
}
\details{
The function performs the following major steps:
\enumerate{
\item Extracts deconvolution subgroups composition from the model.
\item Selects the top features based on average variable importance and direction.
\item Constructs a presence matrix indicating cell type presence across top features.
\item Performs hierarchical clustering on the presence matrix using Jaccard distance.
\item Estimates the optimal number of clusters via silhouette analysis.
\item Evaluates feature importance based on impact on clustering quality via permutation bootstrapping.
\item Generates heatmaps and bar plots summarizing cell presence scores and TF module scores.
}
}
\examples{
\dontrun{
identify.cell.signatures(model = ml_model,
                         deconvolution = deconv_data,
                         var_importance = shap_values,
                         n_top = 20,
                         sign = "Increase",
                         file.name = "example_model")
}
}
