% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CellTFusion.R
\name{compute.TF.network.classification}
\alias{compute.TF.network.classification}
\title{Compute TF Network Classification}
\usage{
compute.TF.network.classification(tf.network, pathways.features, return = T)
}
\arguments{
\item{tf.network}{A list containing TF module eigengenes, typically output from \code{compute.WTCNA()}}

\item{pathways.features}{A matrix with pathway activities, typically from \code{compute.pathway.activity()}}

\item{return}{Logical. If TRUE, intermediate plots (e.g. silhouette, dendrogram, PCA) are saved in the \code{Results/} directory. Default is TRUE.}
}
\value{
A named list of TF module clusters.
}
\description{
Classifies transcription factor (TF) modules into clusters based on their correlations with pathway activity values across samples.
Uses hierarchical clustering and silhouette width to determine the optimal number of clusters.
}
\examples{

data("network.tuto")
data("counts.norm.tuto")
pathways <- compute.pathway.activity(counts.norm)
tfs.modules.clusters <- compute.TF.network.classification(tf.network = network,
                                                          pathways.features = pathways,
                                                          return = FALSE)

}
