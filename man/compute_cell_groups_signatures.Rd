% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CellTFusion.R
\name{compute_cell_groups_signatures}
\alias{compute_cell_groups_signatures}
\title{Compute projected cell group scores on an independent cohort}
\usage{
compute_cell_groups_signatures(
  deconv_res,
  cell_groups,
  features,
  deconvolution_test,
  tfs.module.network,
  batch = NULL
)
}
\arguments{
\item{deconv_res}{A list resulting from \code{multideconv::compute.deconvolution.analysis()} on the training cohort.
This list should include a component named "Deconvolution subgroups composition".}

\item{cell_groups}{A list output from \code{cell.groups.computation()}, containing cell group scores,
feature compositions, and loadings.}

\item{features}{A character vector of selected cell group names to be projected on the test data.}

\item{deconvolution_test}{A matrix or data frame with deconvolution features from an independent
test cohort. Raw (unprocessed) deconvolution output is expected.}

\item{tfs.module.network}{A list from \code{compute.WTCNA()} containing the TF module network information,
used to calculate composite scores for each group.}

\item{batch}{Optional vector indicating batch assignment for samples.}
}
\value{
A data frame with samples as rows and projected cell group scores as columns.
Each column corresponds to one of the selected cell groups in \code{features}.
}
\description{
This function computes the projection of previously defined cell groups
onto an independent test cohort by applying the same composite scoring
strategy used in the training data.
}
\examples{
\dontrun{
cell.groups.projected <- compute_cell_groups_signatures(
    deconv_res = deconv_training,
    cell_groups = cell.groups,
    features = selected_features,
    deconvolution_test = deconv_test,
    tfs.module.network = network
)
}
}
