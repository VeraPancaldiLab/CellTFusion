% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{compute.modules.relationship}
\alias{compute.modules.relationship}
\title{Compute modules relationship}
\usage{
compute.modules.relationship(
  tfs_network,
  matB,
  file_name,
  width = 8,
  height = 8,
  pval = 0.05,
  padj = F,
  cor_type = "p",
  return = F,
  vertical = F,
  plot = T
)
}
\arguments{
\item{tfs_network}{TFs module matrix (samples x modules).}

\item{matB}{matrix B to correlate (samples x features).}

\item{file_name}{string indicating the name of the figure to save.}

\item{width}{integer indicating the width of the figure, default is 8.}

\item{height}{integer indicating the height of the figure, default is 8.}

\item{pval}{p value to use as threshold to differentiate between significant and no significant variables. Default is 0.05.}

\item{padj}{logical value indicating correction of p-value by Bonferroni method has to be applied. Default is false.}

\item{cor_type}{type of correlation to be used. Default is pearson “p”.}

\item{return}{logical value indicating if significant features have to be returned. Default is False.}

\item{vertical}{logical value indicating if function should return a horizontal or vertical plot (this can be useful when looking at several deconvolution features).}

\item{plot}{whether to saved or not the plots.}
}
\value{
A heatmap showing the level of correlation between TFs modules and corresponding features. Only significant features are being shown. If return = TRUE it will return a list with two elements: the correlation matrix between the TFs modules and the other features and a character vector containing the names of the significant associated features. Note that features not significantly associated with any module are not returned.
}
\description{
Performs linear correlation between two matrices of features across samples
}
\examples{

pathways = compute.pathway.activity(counts.norm)
compute.modules.relationship(tfs_modules, pathways, "Pathways_Progeny-TFs_Modules", width = 15)
dt = compute.deconvolution.analysis(deconv, corr = 0.7, seed = 123)
compute.modules.relationship(tfs_modules, deconvolution_matrix, "Deconvolution-TFs_Modules", vertical = T, height = 30, width = 10, pval = 0.05)


}
