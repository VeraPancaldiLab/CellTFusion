% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{compute.survival.analysis}
\alias{compute.survival.analysis}
\title{Compute survival analysis}
\usage{
\method{compute}{survival.analysis}(
  features,
  survival.data,
  time_unit,
  p.value = 0.05,
  thres = 0.5,
  max_factors = Inf
)
}
\arguments{
\item{features}{A matrix with the cell groups scores}

\item{survival.data}{Survival information containing progression free survival information (named as 'PFS') and the binary value indicating death/progression/recurrence (named as 'DRP_st')}

\item{time_unit}{Time unit for variables (either days/months/years)}

\item{p.value}{pvalue for significance. Default is 0.05}

\item{thres}{Threshold for classifying the risk scores. Default is 0.5 (median).}

\item{max_factors}{Maximum number of covariables in the lineal model to consider. Default is Inf, meaning it will test all possible combinations and keep only the significant ones.}
}
\value{
A list with the significant combinations formulas along with their survival curves saved in the Results/ directory.
}
\description{
Computes univariate and multivariate cox proportional hazards (coxPH) models and Kaplan-Meier curves to evaluate across cell type groups. After ﬁtting the CoxPH models to different cell type groups combinations, patient are stratiﬁed based on the linear predictors of the model (risk scores) from which we deﬁne as ‘high’ the patients with risk scores above the median value of the cox model’s linear predictors and as ‘low’ the patients below it. It then performes a Kaplan Meier analysis and plotted the survival curves for each risk group. Finally both survival curves are assessed via a log rank test.
}
\examples{

survival_groups = compute.survival.analysis(features = cell_groups, survival_data, time_unit = "days", p.value = 0.01, max_factors = 3)

}
