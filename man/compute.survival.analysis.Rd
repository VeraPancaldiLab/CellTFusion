% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CellTFusion.R
\name{compute.survival.analysis}
\alias{compute.survival.analysis}
\title{Compute survival analysis using CoxPH and Kaplan-Meier Models}
\usage{
compute.survival.analysis(
  features,
  survival.data,
  time_unit,
  p.value = 0.05,
  thres = 0.5,
  max_factors = Inf
)
}
\arguments{
\item{features}{A numeric matrix or data frame of cell group or module scores. Rows correspond to samples and columns to different features.}

\item{survival.data}{A data frame containing survival information. Must include:
\describe{
\item{PFS_Days}{Numeric vector of progression-free survival times.}
\item{PFS_Outcome}{Binary vector (0/1) indicating progression/recurrence/death (1 = event, 0 = censored).}
}}

\item{time_unit}{A string indicating the time unit used in survival analysis (e.g., \code{"days"}, \code{"months"}, \code{"years"}). This is used for x-axis labeling in Kaplan-Meier plots.}

\item{p.value}{Significance threshold for the log-rank test p-value. Only combinations with p-value below this threshold will be considered significant. Default is \code{0.05}.}

\item{thres}{Threshold used to binarize the risk scores into high and low groups. Default is \code{0.5}, corresponding to the median.}

\item{max_factors}{Maximum number of features to include in a model. Default is \code{Inf}, meaning all combinations will be considered.}
}
\value{
A named list of significant model formulas (as \code{formula} objects), with corresponding Kaplan-Meier plots saved as PDFs in the \verb{Results/} folder.
}
\description{
This function performs survival analysis by fitting univariate and multivariate Cox proportional hazards (CoxPH) models on combinations of the input features. It uses the linear predictors from the CoxPH model to stratify patients into high- and low-risk groups based on a threshold (default the median). Kaplan-Meier survival curves are then generated for each group, and a log-rank test is performed to assess statistical significance. Survival curves are saved as PDF files under the \verb{Results/} directory.
}
